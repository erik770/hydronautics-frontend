import React from 'react';
import {Waiting} from "../components/Waiting/Waiting";

const ProjectsPage = () => {
    return (
        <>
            <Waiting />
        </>
    );
};

export default ProjectsPage;
